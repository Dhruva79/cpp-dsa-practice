344. Reverse String
Easy
Topics
premium lock icon
Companies
Hint
Write a function that reverses a string. The input string is given as an array of characters s.

You must do this by modifying the input array in-place with O(1) extra memory.

Example 1:

Input: s = ["h","e","l","l","o"]
Output: ["o","l","l","e","h"]
Example 2:

Input: s = ["H","a","n","n","a","h"]
Output: ["h","a","n","n","a","H"]



#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
	cout << endl;

	int a[100];

	for (int i = 0; i < n; i++) {
		cin >> a[i];
		cout << endl;
	}
	for (int i = 0; i < n / 2; i++) {
		int temp = a[i];
		a[i] = a[n - 1-i];
		a[n - 1-i] = temp;
		
	}
	for (int i = 0; i < n; i++) {
		cout << a[i] << " ";
	}
}



//reverse string
#include <iostream>
#include <string>
using namespace std;

int main()
{
    string s;
    cin >> s;   // read a string (no need for n separately)

    int n = s.length();

    // Reverse the string in place
    for (int i = 0; i < n / 2; i++) {
        char temp = s[i];
        s[i] = s[n - 1 - i];
        s[n - 1 - i] = temp;
    }

    cout << s; // print reversed string
    return 0;
}
